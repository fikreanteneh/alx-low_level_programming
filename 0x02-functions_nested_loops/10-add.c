#include "main.h"
/**
 * add - returns summation of two integer
 * @x: integer is taken as an argument
 * @y: integer is taken as an argument
 * Return: x + y result
 */
int add(int x, int y)
{
	return (x + y);
}
