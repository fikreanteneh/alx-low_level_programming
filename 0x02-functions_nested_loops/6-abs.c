#include "main.h"

/**
 * _abs - function is used to get the absolute value
 *
 *  @n: can take any number
 * Return: the absolute value of n
 */
int _abs(int n)
{
	if (n < 0);
	{
		a = -n;
		return (a);
	}
	return (n);
}
