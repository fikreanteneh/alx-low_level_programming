#include "main.h"

/**
 * _islower - check if it is in lower case
 *
 * @c: char parameter from the user
 * Return: 0 if uppercase, 1 if lowercase.
 */


int _islower(int c)
{
	if (c >= 'a')
	{
		return (1);
	}
	return (0);
}
