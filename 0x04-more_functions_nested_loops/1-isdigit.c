#include "main.h"
/**
 * _isdigit - check digit
 *
 * @c: character
 * Return: return 1 if a digit else return 0
 */
int _isupper(int c)
{
        if (c >= '0' && c <= '9')
        {
		return (1);
	}
	return (0);

