#include <stdio.h>

/**
 * main - the code begin
 *
 * Return: It always return 0 if the code is correct
 */
int main(void)
{
	/* your code goes there */
	int alpha;

	for (alpha = 97; alpha <= 122; alpha++)
	{
		putchar(alpha);
	}
	printf("\n");
	return (0);
}
